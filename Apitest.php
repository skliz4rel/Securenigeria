<?php

/*

//here, all requisite variables are forwarded to the query_db script to activate myfonetv plan
$url = "http://myfonetv.3waycom.biz/card/query_db.php?SOURCEADDR=$phone&DESTADDR=$shortcode&MESSAGE=$plan&EMAIL=$email&REFERENCE=$reference";
$ch = curl_init($url);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_exec($ch);
curl_close($ch);

if ($ch){
    echo '';
}
	else{ // if the user's no is an non-mtn line or not using mobile/tablet
							  echo 'error in ur processing';
		}

*/

?>